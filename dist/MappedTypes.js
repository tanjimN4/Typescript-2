"use strict";
{
    // mapped types
    const arrOfNumbers = [1, 4, 5];
    const arrOfStr = arrOfNumbers.map((number) => number.toString());
    console.log(arrOfStr);
    const area1 = {
        height: "100",
        width: 50,
    };
}
